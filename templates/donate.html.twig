{% extends 'base.html.twig' %}

{% block main %}
    <h1 class="title">{{ 'about.title'|trans }} {{ app.request.server.get('APP_NAME') }}</h1>
    <a href="{{url}}">
        <span>Pay with crypto</span>
    </a>
{% endblock %}
