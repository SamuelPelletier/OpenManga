homepage:
  path: /
controllers:
    prefix: /{_locale}
    resource: ../src/Controller/
    type: attribute
    requirements:
      _locale: '%app_locales%'

kernel:
    resource: ../src/Kernel.php
    type: attribute
